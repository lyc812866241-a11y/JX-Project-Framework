# Task Spec: GitHub link onboarding

## 目标
用户提供一个仓库链接，即可让具备相应能力的 AI 进入项目访谈和初始化流程，免去每次复制长提示词。

## 上下文
已有 r5 规则与 v0.1.0 本地执行器。当前不足是跨新项目的发现/启动入口，而非用户不会编写指令。本发布保留执行器，补齐公开 README → START_HERE → 规则/工具的路由。

## 范围
新建独立公开仓库 JX-Project-Framework；发布框架代码、适配后的通用规则、启动入口、工具说明、测试和 CI。不修改 AI-Operation，不发布个人学习记录、历史备份、本机配置、真实业务或原始日志。

## 约束
用户明确选择新建公开仓库。GitHub 登录由用户完成，认证不是再批准任务。初始化先了解目标和目录，按实际权限与当前规格执行。裸链接无法技术上保证所有 AI 必然启动，必须披露实测边界。不选定额外开源许可，不冒称已有开源授权。

## 验收
README 首屏可达访谈入口；目标已知/未知、只读审计、目录不明、已有项目、无工具、未实现业务各有正确分流；无个人路径依赖。框架测试通过，脱离原 JX 目录可以构建/运行。发布前扫描允许上传的文件，推送后核对远程 SHA、公开可读 README/START_HERE 及可用测试状态。

## 批准与恢复
用户要求推到 GitHub，并明确选择“新建公开仓库 JX-Project-Framework”。维护者按此范围连续完成。原本地框架保留；新仓库使用独立提交，不覆盖旧库。发布完成记录写 VALIDATION.md，不把准备完毕冒充已推送。
