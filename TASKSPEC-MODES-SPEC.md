# Task Spec：按指定模式转换自然语言

## 目标
用户指定单次、Goal、定时或 Heartbeat，AI 从自然语言生成对应规格，并区分生成与实际启动。

## 上下文
用户已认可公共五项加四种执行配置，明确“是的，补吧”，授权补入公开 JX-Project-Framework。原 K04 只有通用五项，缺模式配置和启动证据路由。

## 范围
新增唯一格式文件，更新根入口、启动、继承及构建示例的相关路由，验证后提交推送本仓库。保留执行器 v0.2.0/r6 检查合同；本次为 TASK_SPEC_MODES_V1 格式增量，来源仍以实际提交 SHA 锁定。

## 约束
不启动 Goal/自动化、不安装 MCP、不改宿主配置或其他项目、不执行完整新会话试跑。选定公共格式加配置，避免四套重复规范；已授权同范围工作不重复索取批准。

## 验收
四种配置与正反例明确；用户指定模式优先；仅整理不启动；未启动不假报；K04 本地引用可继承且隔离构建携带格式；公开路由和组件检查通过，远程提交核验。语义审阅不冒充真实宿主模式启动测试。

## 结果与恢复
当前任务已获上述明确批准；执行结果见 VALIDATION.md 的 TASK_SPEC_MODES_V1 记录。旧运行证据保留其历史范围。
