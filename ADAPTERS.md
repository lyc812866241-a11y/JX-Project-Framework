# 本地与远程调用入口

当前已经实现CLI级gate：重新执行检查，再核对需求覆盖。它不拦截所有宿主操作。接入者必须登记实际使用哪一层，不能把模板存在写成已强制执行。

## 本地执行者

AI从本地规则取得runner和任务ID，在交付前调用 `python <runner> --root <target> gate --task <id>`。任意非零退出码都停止后续提交/发布。用户只说“按框架执行”，无需记参数。

## 已授权的提交前入口

参考脚本见 [templates/project-gate.ps1](templates/project-gate.ps1)。由AI传入实际参数，返回非零立即停止。脚本只验证，不代替用户执行提交或发布。它验证当前工作区，若要验证提交版本，必须另核对暂存/未暂存差异或在精确提交副本运行。

可以在目标已有Hook中调用同样入口，但先读取并保留现有Hook；不要全局设置core.hooksPath，也不要覆盖用户Hook。Git的跳过选项、其他宿主入口和同权限修改仍可能绕过；不能当安全边界。

## CI

框架自身CI模板 [ci.framework.example.yml](templates/ci.framework.example.yml) 运行组件测试、构建和发布边界检查。它不验证下游业务。

目标项目CI需要安装实际测试依赖、取准确业务版本和已审阅任务合同，执行gate；汇总步骤显式拒绝非零/必需跳过/人工待验收。CI不能自动放宽范围或捕获新合同来消除不匹配。分支保护、允许绕过者和CI文件变更权限另按目标仓库设置。

本轮不启用远程Actions、不扩展GitHub登录scope、不配置分支保护。以上材料已具备接入路径，但远程状态仍为未接入，完整试跑按该边界判断，不把它列为已实测强制门禁。
