# 融合对照与取舍

此处“融合”指选择性借鉴并在本框架独立实现，不是安装四套运行时，也不声称覆盖它们的全部功能。读取日期、固定提交、原文件链接与内容摘要见 [sources.lock.json](sources.lock.json)。原源码仅在临时目录审阅，没有复制代码/技能、安装 Hook 或导入个人权限；四个所审仓库根许可证均为 MIT。

| 来源与能力 | 实际采用 | 本地落点 | 组件验证 | 边界/不采用 |
|---|---|---|---|---|
| Superpowers：完成前先验证 | 新鲜证据先于完成声明；检查后改文件使证据过期 | core.verify/finish/gate；W02 | test_A04、A07、A08 | 不照搬“每条回复都必须重跑”，以输入/执行器摘要及验证时点判断 |
| Superpowers：系统排错 | 复现→直接原因→上游机制→检出缺口→修复→原例/反例 | 通用规则5.1；DELIVERY_LOOP | test_A09_A10；因果解释由AI审阅 | 未证实根因不伪造；允许必要止损，止损不算根治 |
| Superpowers：可交接的小任务计划 | 一个任务有明确结果、文件范围、依赖、验证与交接 | Task Spec、DELIVERY_LOOP | 规则场景审阅；范围反例A13 | 不自动创建子代理、不要求固定目录或每步提交 |
| GSD：持续状态与恢复 | 持久阶段/负责人/下一步，历史与当前分开，原子写入与锁 | core.checkpoint/status | LifecycleTests；A15 | 非完整任务DAG/并行调度器；项目业务状态仍在已绑定事实源 |
| GSD：对话验收与缺口回流 | 一次展示一项预期，保存用户真实反馈，缺口变成修复任务 | DELIVERY_LOOP人工验收；FULL_TEST_PLAN | 流程审阅；manual负例 | 不把空回复、沉默或AI自报算通过；无可信人类身份仍pending_manual |
| Agent Execution Harness：声明需要证据 | 需求逐项关联检查并展示实际结果，完成检查拒绝缺项 | core.coverage/finish/gate | coverage正反例；A03/A06/A08 | 不复制评分系统；测试数量不等于需求完整 |
| Agent Execution Harness：范围与恢复条件 | 当前任务差异核对、共享接口单一所有者、恢复前保留修改 | core.contract/installation/restore；W03/W05 | A02/A13/A15 | 文件范围检查不是系统权限隔离；远程门禁未自动启用 |
| AI-Operation：可调用验收及安装入口 | 保留统一CLI思路，命令固定、报告实际采集、升级有差异/恢复 | init/doctor/task/verify/finish/upgrade/restore | A01—A16组件覆盖 | 重写执行层；不复制MCP/IDE配置、机器路径、fast-track标记 |
| AI-Operation：旧实现问题作为负例 | 不接受全跳过+文字PASS、旧VERIFIED或无截图说明 | core.execute/finish | A05/A08/A14；此前隔离函数复现 | 完整旧MCP/Hook生命周期未复现；不称修好了旧仓库 |

## 组合后的一条实际调用链

链接访谈 → 五项规格与本地规则 → 当前任务/写集冻结 → 实施或根因修复 → 同步状态/地图 → gate执行并核对 → coverage逐项交付 → checkpoint保存 → status恢复。

任务进度记录在 `.artifacts/control/<task>`，不会通过修改源码摘要使刚完成的验证自动失效；业务状态、地图、配置仍属于源码输入，改动后必须重验。进度“complete”仅在当前finish=ready时可写，人工项未通过不能用进度记录绕过。

## 明确裁决的冲突

1. 用户操作成本优先：不让用户提供技术字段、命令或长提示词；AI负责转换。
2. 只维护一套规则：上游是设计依据，运行时只使用项目本地适配和本执行器。
3. 一次授权覆盖本轮范围：不采用固定每阶段再次批准；新增业务目标/外部副作用仍要明确授权。
4. 不机械全量测试，也不随意省略：固定重要回归＋当前变更检查；影响未知时扩大覆盖并声明缺口。
5. 自动检查与业务判断分开：组件pass、用户理解、人工验收、远程部署是不同事实。
6. 不复制隐式同意：空回复/沉默不算通过；不能用字符串伪造人工身份。
7. 不默认多代理：明确请求且共享写集可分离时才使用；子任务自报必须由集成方复核。
8. 不声称绝对强制：已实现本地可调用gate；宿主Hook/远程CI接入是条件配置，按实际状态登记。

## 仍保留的领域边界

不引入完整GSD规划运行时、自动影响分析、模型调度、技能自动抽取、多IDE安装器、同权限防篡改、人类身份系统。它们不是本轮已选定需求。完整新项目试跑按 [FULL_TEST_PLAN.md](FULL_TEST_PLAN.md) 另行执行，本轮仅完成准备和组件验证。
