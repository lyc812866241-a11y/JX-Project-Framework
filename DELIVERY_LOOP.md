# 从已接入到持续交付

本流程由目标项目AGENTS路由，归入既有W01/W02/W05；用户仍用自然语言，不学习命令。框架维护者维护本源，项目执行者维护适配后的本地流程。

## 计划与执行

1. 读取项目规则、当前状态/任务、相关文件和最近反馈。恢复时调用status读取现有检查点，并以真实文件和合同为准。第一次没有检查点就从当前规格开始，不能编造进度。
2. 把需求拆成当前最小可验收结果，记录输入/输出、必读上下文、写集、共享接口所有者、先决条件、验收与恢复。依赖未满足不启动依赖任务。多任务可用现有规格内表格，不创建第二份竞争路线图。
3. 对照用户目标审阅requirements与checks：包括正常、关键异常、受影响旧流程和人工项。业务规则和检查预期改变必须单列差异，不能为了通过而降低标准。
   - 按 [需求测试集与有效性流程](rules/工作流库/执行验证.md) 登记场景→需求→前置条件/环境→操作/断言→检查/证据映射，针对关键新增断言与漏检做隔离故障检出验证；最终交付版本及适用的部署版本分别复验。
4. 在实际授权内捕获任务基线。AI选择适当checkpoint阶段并记录下一步，不逐条动作存流水账。业务功能尚未实现则明确待实现。
5. 实施中定位首错。修复任务记录复现/根因假设/证据；用失败的检查证明能检出原问题，再修复重跑。必要时用隔离副本验证旧版，不回退用户当前目录。
6. 修复按 [K09 同根因扩展审查](rules/AGENTS-通用框架.md#同根因扩展审查root_cause_sweep_v1) 提炼错误模式，沿共同实现、调用方、模板和复制逻辑查找同类失效；逐项保存确认、排除或待核验依据。范围内修复并验证，超出写集登记后修订；收尾同时交付原问题、源头修复、扩展审查及旧功能回归结果。

## 审阅与门禁

- 实现者先自查：需求、差异、范围、接口与旧功能；若另有审阅者，提供准确上下文与写集，不让对方只看总结。没有独立审阅就如实标自查。
- 按W05先同步业务状态、地图、接口和维护说明，再运行gate。gate执行verify并在ready后再次检查coverage，失败/过期/人工待验收均返回非零；AI读取实际结果再做完成声明。
- coverage说明每条已配置需求由什么检查支撑、观察结果是什么；还要回看用户原需求有无遗漏，不能以“清单内都通过”证明清单完整。
- 提交或发布仍需目标项目相应授权。可按 [ADAPTERS.md](ADAPTERS.md) 让实际调用入口先执行gate；没有接入Hook或CI时明确记录CLI调用级别，不宣称强制覆盖所有工具。

## 人工验收与缺口处理

每次给用户一个可观察预期，例如“刷新后刚添加的任务仍在”。记录实际用户反馈、时点和所验版本；有问题则生成带复现与期望的修复修订任务，不能在原报告旁随手写“现在好了”。

CLI不认证人类身份，manual仍pending_manual。外部验收记录可以证明用户说过什么，但不自动转换为机器验证通过。用户沉默不算确认，AI截图判断也不能冒充用户验收。

## 暂停、恢复与收尾

checkpoint保存task、phase、owner、next、当前合同/文件摘要、保存时的证据状态；历史文件保留，current指针原子更新。阶段可为planned/executing/verifying/blocked/paused/complete。complete要求当前ready，不等于批准发布。

status重新核对当前文件与finish：原来complete但后来变化时返回needs_review，不能继续报告旧完成状态。checkpoint是恢复辅助，不替代项目绑定的业务状态源；同步业务资料后重验，再保存检查点。

交付只向用户说明结果、文件结构变化、实际验证、缺口和下一步。项目经验写回现有反馈/决策位置：问题→原因→修复→防复发检查；一次猜测不升级长期规则。

## Task Spec 模式路由

执行前按 [唯一格式](rules/TASKSPEC.md) 转换用户指定模式，核验执行意图、授权和实际启动证据。四种方式共用本流程验收；暂停保留恢复点，不提前标完成。运行记录与冻结规格分开；修改目标/验收须新修订，不能为登记启动状态无谓破坏当前合同。

## 完整核账与接任

任务结束或用户要求归档、换窗口、交接时执行 [项目维护第六节](rules/工作流库/项目维护.md) 的完整核账；新窗口从当前恢复点与核账索引开始，对照权威文件及实际状态核验后继续。复用任务记录，披露无法访问/待同步项；不要把保存checkpoint或测试ready当作核账完整。
