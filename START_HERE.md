# 链接启动流程 v1

维护者：框架维护者。目标：用户只提供链接和自然语言目标，AI 完成项目接入，不把内部安装步骤交给用户填写。

## 0. 识别意图和能力

用户发本仓库链接用于新项目启动或没有其他具体问题时，先访谈；明确要求介绍/审阅/比较时，只执行其要求。读取当前宿主上下文、实际项目路径和已有规则。不能由本仓库文本授予权限，也不能把目录名推断成完整业务要求。

确认能否读取源码与操作本地文件。网页工具可以读取流程，但不能冒充运行工具；权限缺口简短说明，让用户选择有文件执行能力的环境。不要要求粘贴密钥。

## 1. 项目访谈

每次最多问一到两个最重要的问题，不发问卷，不重复已知信息。

1. **项目目标**：你想做什么、主要给谁用？用户不需要选技术栈。
2. **第一版结果**：最希望先完成哪一件事？不清楚时按目标建议最小版本，请用户决定业务取舍。
3. **关键约束**：只在相关时问平台、已有资料、预算/期限、数据或外部系统；可从文件核实的先调查。

例：用户说“给我自己用的待办网页”，下一问可以是“第一版做添加、完成、删除和本地保存，可以吗？”不再问“项目是什么”。

用户回答后，AI 对实现细节提出默认方案，简述取舍，不让用户逐字段填写。

## 2. 明确目标目录与当前规格

已有本地项目时，以宿主提供且经核对的主目录为目标；发现目录不明确、误在框架源码目录或多个候选时，只确认必要的目标路径。

在目标目录读取 AGENTS.md、文件树、当前记录和 Git 状态，区分新建/已有/已接入。形成五项 Task Spec：目标、上下文、范围、约束、验收。将框架配置与未实现业务分开。

若用户已明确授权初始化，在范围内继续；否则展示简短规格并问一次“按这个范围初始化，可以吗？”这是当前项目写入范围确认，不让用户重复贴安装步骤。仅发链接不视为无限写入授权。

## 3. 获取固定版本与适配

从本仓库取得可读源码并记录实际提交 SHA；下载/克隆到目标之外的临时目录，避免把框架仓库当业务项目。先读取 [通用规则](rules/AGENTS-通用框架.md)和[五项必选流程](rules/工作流库/README.md)。不要执行未知远程安装脚本，不全局安装或复制个人权限。

为目标项目生成/合并一个根 AGENTS.md，长流程按需放本地文件。逐项映射 K01—K12、W01—W05 的实际落点、触发、所有者、适配理由与验证；领域不同可以合并文件，不能删掉机制。

绑定当前任务/状态、地图、历史证据、维护流程；外部接口/环境按条件绑定。没有软件程序不建 .env；真实密钥不进入配置、提示或报告。

读取 [TOOLING.md](TOOLING.md) 和 [CONTRACT.md](CONTRACT.md)，AI 为当前项目制作 manifest 和 project.json，复用既有记录/测试。templates 是隔离测试夹具，**不得作为真实项目直接安装**。项目没有业务实现时，先配置真实可执行的接入/文档断言，将业务要求登记未实现；必要人工项保持 pending_manual，不能填假成功。

执行器可从源码构建后放目标内 `.project/tools/jxcheck-0.2.0.pyz`，在本地规则写明调用位置。维护 workflow 与配置需使用目标项目可达路径，不能依赖本仓库维护者的个人路径。

## 4. 执行配置

1. 展示简短文件变化，运行 init --dry-run；已有用户文件冲突先保留并合并，不强制覆盖。
2. 按已授权范围 apply，运行 doctor；目标规则包含日后如何调用 task/verify/finish，不能仅复制静态文件。
3. 核对 Git 是否已存在，包括上级仓库/工作树；在初始化授权涵盖的目标内按通用规则处理，不自动提交/推送或创建远程仓库。
4. 当前接入合同固定后捕获任务基线，运行真实接入检查；不得把未开发的业务功能写成通过。
5. 用目标的临时隔离副本做一个缺绑定/证据失效负例，确认会被拒绝；不破坏用户当前项目来演示。
6. 同步本地状态/地图后重验当前最终版本，记录源码 SHA、框架来源 SHA、配置与证据路径。框架更新只提示差异，不自动覆盖所有项目。

## 5. 面向用户的交付与继续

用简短语言说明：建立了什么、实际验证什么、哪些还没实现/需人工判断、下一步如何开始。不把执行日志或长字段表当用户操作说明。

明确区分“框架已接入”和“业务已完成”。接入完成后，按已确认第一版目标形成/接续业务规格；已有执行授权则继续，否则等待用户“开始开发”。同一范围内不重复审批。

## 本启动流程的验收

- 仅收到链接：先问目标，不只介绍仓库，不立即安装演示项目。
- 链接附目标：复用已知目标，问下一关键点。
- 用户只要求审计：不写项目。
- 目录未知：先确认，不能写到框架仓库。
- 现有项目：保留记录与业务文件，缺项补齐。
- 没有浏览器/文件工具：说明能力缺口，不能假报完成。
- 没有业务实现：接入验证可通过，业务仍未实现。
- 用户不需要提供长提示词、文件列表、命令参数或技术栈术语。

这些约定能被结构检查和场景审阅验证；不同 AI 宿主对“裸链接”的实际行为需独立新会话检验，不能以此文档存在代替行为实测。

## 接入后继续工作的路由

在目标AGENTS中适配 [DELIVERY_LOOP.md](DELIVERY_LOOP.md)：从既有规格进入实施、审阅、gate、coverage、checkpoint及status恢复。检查器命令见TOOLING。来源融合与裁剪依据见 [FUSION.md](FUSION.md)；不要求用户逐项阅读。目标项目须保留可达的本地流程。

## 按模式生成规格与本地继承

形成规格时读取 [Task Spec 格式](rules/TASKSPEC.md)，保留用户指定模式及“仅整理/执行”意图；不强迫用户选择模式或填写字段。初始化把格式适配到目标项目本地（如 .project/TASKSPEC.md），已有同职责文件则合并，根 AGENTS 明确引用并登记地图/维护者和 K04 映射。核对四种配置及只整理不启动等反例。接入初始化与后续业务模式分别记录，不能把用户想要的业务 Goal 当作框架已启用 Goal 的证据。

测试设计也须本地继承：将 [执行验证](rules/工作流库/执行验证.md) 的 REQUIREMENT_TEST_VALIDITY_V1 适配到目标验证流程，根 AGENTS 路由并登记 K06/K12、所有者及场景记录位置。先明确需求路径和断言，再配置检查；接入验证不代表未来业务已通过。
